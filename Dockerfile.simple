# Dockerfile ultra-simple pour éviter les problèmes npm ci
FROM node:18-alpine

# Répertoire de travail
WORKDIR /app

# Copier tout le backend d'un coup
COPY backend/ ./backend/

# Aller dans le répertoire backend et installer
WORKDIR /app/backend
RUN npm install --production

# Créer les dossiers nécessaires
RUN mkdir -p uploads temp backups logs

# Exposer le port
EXPOSE 10000

# Variables d'environnement
ENV NODE_ENV=production
ENV PORT=10000

# Démarrage
CMD ["node", "server.js"]